<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
        <!-- 2026 güncellemesi — AdMob geçiş (interstitial) reklamları tam
             ekranı kaplamıyor bug'ı için. `compileSdk = 37` (bkz.
             build.gradle.kts) Android 15+'ın ZORUNLU edge-to-edge
             davranışını devreye sokuyor — bu, `google_mobile_ads`'ın kendi
             reklam Activity'sinin durum çubuğu alanını doğru
             kapatamamasına yol açıyor (reklamın ÜSTÜNDE uygulamamızın eski
             arayüzü/AppBar'ı görünür kalıyor) — Google'ın kendi AdMob SDK
             destek ekibinin RESMİ olarak önerdiği geçici çözüm bu (bkz.
             CLAUDE.md "AdMob Entegrasyonu" bölümü, kaynak linki dahil). Bu
             Android 15 API'si (`windowOptOutEdgeToEdgeEnforcement`)
             uygulamayı bu ZORUNLU davranıştan çıkarıp GELENEKSEL (opak
             durum çubuğu) davranışa döndürüyor — hem `MainActivity` hem
             AdMob'un kendi Activity'leri için düzeltiyor. Eski Android
             sürümlerinde bu öğe sessizce yok sayılır (API 35'te eklendi),
             zararsız. **GEÇİCİ bir çözüm — Google, bu bayrağın Android
             16'yı hedefleyen uygulamalarda DEVRE DIŞI/kullanılamaz
             olacağını duyurdu** — o noktada AdMob SDK'sının kendisinin
             inset'leri doğru işlemesi gerekecek, bizim tarafımızdan
             yapılacak ek bir şey yok ama `targetSdk` Android 16'ya
             yükseltilince bu bug'ın geri gelip gelmediği TEKRAR
             kontrol edilmeli. -->
        <item name="android:windowOptOutEdgeToEdgeEnforcement" tools:targetApi="35">true</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">#FFF8E8</item>
        <item name="android:windowOptOutEdgeToEdgeEnforcement" tools:targetApi="35">true</item>
    </style>
</resources>
